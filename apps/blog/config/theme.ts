const colors = {
  green: '#00DA91',
  black: '#000',
  white: '#fff',
  gray: '#cccc',
};

const theme = {
  '$brand-primary-color': colors.green,
  '$btn-primary-bg': colors.green,
  '$btn-primary-color': colors.black,
  '$block-primary-border': colors.gray,
  '$block-border-radius': 5,
};

export default theme;
